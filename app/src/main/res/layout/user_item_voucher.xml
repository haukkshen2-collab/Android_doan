<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginStart="@dimen/space_sm"
    android:layout_marginTop="@dimen/space_sm"
    android:layout_marginEnd="@dimen/space_sm"
    android:background="@drawable/bg_card_accent"
    android:elevation="@dimen/elevation_sm"
    android:foreground="?attr/selectableItemBackground"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:padding="@dimen/card_padding">

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/lblTenVoucher"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Voucher"
            android:maxLines="1"
            android:ellipsize="end"
            style="@style/App.Text.Title" />

        <TextView
            android:id="@+id/lblMoTaGiam"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/space_xxs"
            android:text="Giảm 0%"
            style="@style/App.Text.Price" />
    </LinearLayout>

    <ImageView
        android:layout_width="@dimen/icon_xl"
        android:layout_height="@dimen/icon_xl"
        android:background="@drawable/bg_icon_tron_cam"
        android:contentDescription="Voucher ưu đãi"
        android:padding="@dimen/space_md"
        android:src="@drawable/ic_voucher"
        android:tint="@color/mauCamDam" />
</LinearLayout>
